'use strict';

//leading 0's are removed
let oddsOnly = function () {
  let newArray = [];
  for (var i = 0; i < 1,000,000; i++) {
    if (array[i] % 2 === 0) {
    newArray.pop();
    } else {
    newArray.push();
    }
  }
};

let palindromFinder = function () {
  let results;
  for (var i = 0; i < newArray.length; i++) {
    if (newArray[i] === newArray[i].reverse()) {
      return results;
    }
  }
};

let sumOfResult = function () {
  result =+ results
  return result;
};

module.exports = () => {
  return true;
};
